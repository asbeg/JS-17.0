/*        let cloneExpensesItem = expensesItems[0].cloneNode(true);
        expensesItems[0].parentNode.insertBefore(cloneExpensesItem, expensesAdd);
        expensesItems = document.querySelectorAll('.expenses-items');
        if (expensesItems.length === 3) {
            expensesAdd.style.display = 'none';

        },*/

/*        if (expensesItems.length === 3){
            expensesAdd.style.display = 'none';
        }else {
            let newEl = document.createElement('input');
            newEl.innerHTML = `
                    <input type="text" class="income-title" placeholder="Наименование">
                `;
            document.querySelector('.expenses-items').appendChild(newEl);
        }*/

/*        let cloneExpensesItem = expensesItems[0].cloneNode(true);
        expensesItems[0].parentNode.insertBefore(cloneExpensesItem, expensesAdd);
        expensesItems = document.querySelectorAll('.expenses-items');
        if (expensesItems.length === 3) {
            expensesAdd.style.display = 'none';
        }*/

/*
            let el, s;
            el=document.querySelector('.income-title');
           // s=el.innerHTML;
            s=`<input type="text" class="income-title" placeholder="Наименование">
                    <input type="text" class="income-amount" placeholder="Сумма">`;
            el.insertAdjacentHTML('afterend', '<input type="text" class="income-title" placeholder="Наименование">');
*/